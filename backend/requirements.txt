# Production dependencies — install with: pip install -r requirements.txt
# Dev-only tools (debug toolbar, ipython) should be installed manually in dev envs.

# Django core
django>=6.0
djangorestframework>=3.15
psycopg2-binary>=2.9
python-decouple>=3.8

# Auth & CORS
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3

# API docs & filtering
drf-spectacular>=0.27
django-filter>=24.0

# ML stack
scikit-learn>=1.4
pandas>=2.2
numpy>=1.26
imbalanced-learn>=0.12
joblib>=1.3

# Testing
pytest>=8.0
pytest-django>=4.8
coverage>=7.4

# Production server
gunicorn>=21.2
whitenoise>=6.6
